# =============================================================================
# .gitignore — Project: bybit-arb-bot (v2, audit add-ons for secrets)
# Purpose: Keep repo clean; prevent secrets and local artifacts from entering Git.
# =============================================================================

# --- Python bytecode / caches -------------------------------------------------
__pycache__/
*.py[cod]
*.pyo
*.pyd
.pytype/
.mypy_cache/
.pyre/
.pyright/

# --- Pytest / Coverage --------------------------------------------------------
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.coverage-results/
coverage.xml

# --- Virtual environments -----------------------------------------------------
.venv/
venv/
env/

# --- Logs / Temp / Runtime ----------------------------------------------------
*.log
logs/
run/
tmp/
temp/
dev/tmp/
*_tmp/
*.tmp
*.bak
*.old
*.orig
*.swp
~*
# Local run logs / artifacts
_runlogs/
artifacts/

# --- OS / Editor junk ---------------------------------------------------------
.DS_Store
Thumbs.db
Icon?
.vscode/
.idea/

# --- Build / Dist / Packaging -------------------------------------------------
dist/
build/
*.egg-info/
.eggs/

# --- Data artifacts (local only; keep examples/schemas elsewhere) ------------
data/**/*.db
data/**/*.sqlite
data/**/*.sqlite3
data/**/*.parquet
*.db
*.sqlite
*.sqlite3
*.parquet

# --- Notebooks checkpoints ----------------------------------------------------
.ipynb_checkpoints/

# --- Env/Secrets files (NEVER commit secrets) --------------------------------
# root-level
.env
.env.*
# any folder
*.env
*.env.*
*.secrets
*.secrets.*
# config-specific
config/*.env
config/*.env.*
config/*.secrets
config/*.secrets.*
# allowlist examples
!.env.example
!config/example.config.json

# --- Reports / exports (local) -----------------------------------------------
reports/**/*.html
reports/**/*.xlsx
reports/**/*.csv
# Generated digests (local)
local-digest.*
digest-*.json
digest-*.md

# --- GitHub Actions local artifacts (if any) ---------------------------------
.github/workflows/*.local.*

# --- Node / Frontend (if ever used) ------------------------------------------
node_modules/

# =============================================================================
# End of .gitignore (v2)
# =============================================================================
coverage_phase4.xml
